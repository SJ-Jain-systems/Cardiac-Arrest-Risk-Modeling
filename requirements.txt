# NOTE: pyproject.toml is the source of truth for dependencies.
# This file is kept for convenience (`pip install -r requirements.txt`) and
# mirrors the pinned versions declared under [project.dependencies] and
# [project.optional-dependencies].dev in pyproject.toml. Keep them in sync.
#
# Preferred install:  pip install -e ".[dev]"

# --- Core analysis stack ([project.dependencies]) ---
pandas==2.2.3
numpy==2.2.6
matplotlib==3.10.3
seaborn==0.13.2
scikit-learn==1.6.1
scipy==1.15.3
statsmodels==0.14.4
shap==0.47.2
joblib==1.5.1
jupyter==1.1.1

# --- Development / tooling ([project.optional-dependencies].dev) ---
pytest==8.3.5
ruff==0.11.13
black==25.1.0

# --- RAG (optional) ([project.optional-dependencies].rag) ---
# Install with: pip install -e ".[rag]"
# Retrieval needs no API key; generation uses the Claude API (ANTHROPIC_API_KEY).
sentence-transformers==3.3.1
anthropic==0.69.0
